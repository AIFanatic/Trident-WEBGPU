{
  "name": "trident-webgpu",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild src/index.ts --bundle --outfile=dist/trident.js --loader:.wgsl=text --loader:.obj=file --loader:.png=file  --target=es2022 --format=esm --watch"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "esbuild": "^0.21.2"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "wgpu-matrix": "^3.0.1"
  }
}
